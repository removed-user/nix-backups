Wanted a module to enumerate/search/filter files -
Which didn't duplicate inputs, use blind merges in a fixed-point, and have infinite recursion
Which I could use for convinient imports
Put the hoisted/lifted attrs in a different attrset
using a seperate evalModules call, running before the main eval
that would make it usable for non-infinitely recursing imports and "is the way"

It also doesn't exist
Even though filesystem operations are one of the most universal requirements of any programming language
Especially one thats meant to configure an entire computer.
